apiVersion: v1
kind: Service
metadata:
  name: link
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: link
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: tcp-8080

