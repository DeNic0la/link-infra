apiVersion: v1
kind: Service
metadata:
  name: link
  labels:
    service.kubernetes.io/service-proxy-name: "link-ui"
spec:
  selector:
    app: link-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000

