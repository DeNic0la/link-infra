apiVersion: v1
kind: Service
metadata:
  name: link
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    service.kubernetes.io/service-proxy-name: "link-ui"
spec:
  selector:
    app: link-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000

