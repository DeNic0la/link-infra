apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: link
  name: router
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-issuer
    cert-manager.io/common-name: link-dev.denic0la.ch
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - link-dev.denic0la.ch
      secretName: link-dev-tls
  rules:
    - host: link-dev.denic0la.ch
      http:
        paths:
          - path: "/api"
            pathType: Prefix
            backend:
              service:
                name: link-back
                port:
                  number: 80
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: link-ui
                port:
                  number: 8080
